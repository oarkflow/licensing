PHP Offline SDK Example

This example shows how to verify an offline signed bundle and sync the revocation manifest.

Usage:
  php index.php --server http://localhost:6601 --bundle /path/to/bundle.json --cache /tmp/licensing-cache
